#include <stdio.h>
/**
 * main - entry point
 *
 * Return: 0
 *
 */
int main(void)
{
	char lowercase, uppercase;

	/* Print lowercase letters */
	for (lowercase = 'a'; lowercase <= 'z'; lowercase++)
	{
		putchar(lowercase);
		letter++;
	}

	/* Print uppercase letters */
	for (uppercase = 'A'; uppercase <= 'Z'; ippercase++)
	{
		putchar(uppercase)
	}

	/* Print new line */
	putchar('\n')

	return (0);
}
